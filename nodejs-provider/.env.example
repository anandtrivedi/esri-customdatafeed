# Databricks Custom Data Feed Provider - Environment Configuration
# Copy this file to .env and fill in your values.
# Per-table settings (tableName, geometryColumn, etc.) are configured
# per-service via the Admin REST API createService call, not here.

# ---------------------------------------------------------------
# Databricks Connection (REQUIRED for Lakehouse read-only services)
# ---------------------------------------------------------------
DATABRICKS_SERVER_HOSTNAME=your-workspace.cloud.databricks.com
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/your-warehouse-id
DATABRICKS_ACCESS_TOKEN=dapi_your_token_here

# ---------------------------------------------------------------
# Lakebase Connection (OPTIONAL - for editable services)
# ---------------------------------------------------------------
# If omitted, OAuth tokens are auto-generated using the PAT above
# via the Databricks /api/2.0/database/credentials endpoint.
# Set explicitly only if auto-generation doesn't work for your setup.
# LAKEBASE_PASSWORD=your-oauth-token-or-password
# LAKEBASE_USER=databricks

# ---------------------------------------------------------------
# Query Defaults (OPTIONAL)
# ---------------------------------------------------------------
# DATABRICKS_SRID=4326
# DATABRICKS_MAX_RECORD_COUNT=2000
# DATABRICKS_QUERY_TIMEOUT=120000

# ---------------------------------------------------------------
# Connection Pool Tuning (OPTIONAL)
# ---------------------------------------------------------------
# DATABRICKS_POOL_MIN=2
# DATABRICKS_POOL_MAX=10
# LAKEBASE_POOL_MIN=2
# LAKEBASE_POOL_MAX=10
# LAKEBASE_SSL_VERIFY=false

# ---------------------------------------------------------------
# Security (OPTIONAL)
# ---------------------------------------------------------------
# ArcGIS user authentication - validates req._user from CDF runtime
# ENABLE_USER_AUTH=false

# Audit logging - writes JSON lines to log file
# ENABLE_AUDIT_LOG=false
# AUDIT_LOG_FILE=./logs/audit.log
